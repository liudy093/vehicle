apiVersion: v1
kind: Service
metadata:
  name: mapping-cloud
  namespace: argo
  labels:
    app.kubernetes.io/name: mapping-cloud
    app.kubernetes.io/part-of: mapping-cloud
spec:
  type: NodePort
  selector:
    app: mapping-cloud
    app.kubernetes.io/name: mapping-cloud
  ports:
    - name: ingest
      port: 15000
      targetPort: 15000
      nodePort: 30404
    - name: viewer
      port: 18080
      targetPort: 18080
      nodePort: 31278
